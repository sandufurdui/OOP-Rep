package Lab1;

import java.util.ArrayList;

public class Main {
    public static void main(String[] args){
       /* Monitor asus = new Monitor("black", 123, 123, 15.6, 20);
        Monitor acer = new Monitor("blue", 135, 167, 15.6, 25);

        if(asus.getWidth() > acer.getWidth()){
            System.out.println(asus.getWidth() + ">" + acer.getWidth());
        }else if(asus.getWidth() < acer.getWidth()){
            System.out.println(asus.getWidth() + "<" + acer.getWidth());
        }else{
            System.out.println(asus.getWidth() + "=" + acer.getWidth());
        }
        if(asus.getHeight() > acer.getHeight()){
            System.out.println(asus.getHeight() + ">" + acer.getHeight());
        }else if(asus.getWidth() < acer.getWidth()){
            System.out.println(asus.getWidth() + "<" + acer.getHeight());
        }else{
            System.out.println(asus.getHeight() + "=" + acer.getHeight());
        }
        if(asus.getResolution() > acer.getResolution()){
            System.out.println(asus.getResolution() + ">" + acer.getResolution());
        }else if(asus.getResolution() < acer.getResolution()){
            System.out.println(asus.getResolution() + "<" + acer.getResolution());
        }else{
            System.out.println(asus.getResolution() + "=" + acer.getResolution());
        }
            if(asus.getDiagonal() > acer.getDiagonal()){
            System.out.println(asus.getDiagonal() + ">" + acer.getDiagonal());
        }else if(asus.getDiagonal() < acer.getDiagonal()){
            System.out.println(asus.getDiagonal() + "<" + acer.getDiagonal());
        }else{
            System.out.println(asus.getDiagonal() + "=" + acer.getDiagonal());
        }}*/

        University  UTM = new University("UTM", 1967),
                ULIM = new University("ULIM", 854),
                ASEM = new University("ASEM", 1350);

    }
}
